@model IEnumerable<Notlarim102.Entity.Category>

@{
    ViewBag.Title = "Index";
}

<br />

<p>
    @Html.ActionLink(" ", "Create", null,new { @class = "btn btn-outline-primary far fa-copy" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedOn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModifiedOn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModifiedUserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedOn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModifiedOn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModifiedUserName)
        </td>
        <td>

            @*<button class="btn btn-primary" type="submit">@Html.ActionLink("Details", "Details", new { id = item.Id })</button>
            <input class="btn btn-primary" type="button" value="@Html.ActionLink("Delete", "Delete", new { id = item.Id })">*@

            <div class="btn-default btn-group-justified" role="group" aria-label="...">
                @Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "btn btn-outline-primary far fa-edit" })
                @Html.ActionLink(" ", "Details", new { id = item.Id }, new { @class = "btn btn-outline-success bi bi-search" })
                @Html.ActionLink(" ", "Delete", new { id = item.Id }, new { @class = "btn btn-outline-danger bi bi-trash-fill" })

            </div>
        </td>
    </tr>
}

</table>
